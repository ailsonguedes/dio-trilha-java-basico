package edu.ailson.sextasemana;

public class PalavrasReservadas {
    //Link: https://glysns.gitbook.io/java-basico/sintaxe/palavras-reservadas

    // Controle de Pacotes:
    // importa pacotes ou classes para dentro do código;
    // package: especifica a que pacote, todas as classes de um arquivo pertencem.

    // Modificadores de Acesso:
    // public: acesso de qualquer classe;
    // private: acesso apenas dentro da classe;
    // protected: acesso por classes no mesmo pacote e subclasses.

    // Tipos Primitivos:
    // boolean: um valor indicando verdadeiro ou falso;
    // byte: um inteiro de 8 bits (signed);
    // char: um character unicode (16-bit unsigned);
    // double: um número de ponto flutuante de 64 bits (signed);
    // float: um número de ponto flutuante de 32 bits (signed);
    // int: um inteiro de 32 bits (signed);
    // long: um inteiro de 64 bits (signed);
    // short: um inteiro de 32 bits (signed);
    // void: indica que o método não tem retorno de valor.

    // O CONTEÚDO COMPLETO A RESPEITO DAS PALAVRAS RESERVADAS PODE SER ACESSADO NO LINK


    
}
